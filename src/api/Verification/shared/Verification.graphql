type Verification {
  id: Int!
  target: String!
  payload: String!
  key: String!
  verified: Boolean!
  createAt: String!
  updateAt: String
}

# v = Verification
# v.target = phone
# v.payload = +24214242142
# v.key = 3421

# hello vrify with the number 3421

# 3421

# v(payload: +24214242142, key: 3421)